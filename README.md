# Pomodoro Timer (CLI + REST API)

Небольшой **пет-проект**, реализующий концепцию **помодоро-таймера**, созданный для практики и демонстрации навыков backend-разработки.

- есть **CLI-таймер**, который считает пользовательские сессии  
- каждая сессия сохраняется в **PostgreSQL**  
- поверх этого сделан **REST API на FastAPI**  
- всё упаковано в **Docker + docker-compose**  
- написаны **юнит-тесты (pytest)**  
- в логике таймера используется **многопоточность**

Проект сделан как учебный, чтобы отточить навыки работы с **Python**, **SQL**, **REST API**, **Docker** и написанием юнит-тестов.

---

## Стек

- Python 3.10  
- FastAPI  
- PostgreSQL 16  
- SQLAlchemy  
- Pytest  
- Docker, Docker Compose  
- WSL2 (под Windows)

---

## ⚙️ Как запустить

### Вариант 1 — через Docker (рекомендуется)

Требуется установленный **Docker** / **Docker Desktop**.

### Вариант 2 - Из корня проекта:

```bash
docker compose up --build
```


## 📂 Структура проекта

app/
├── api.py           # FastAPI эндпоинты
├── db.py            # Подключение к БД
├── models.py        # SQLAlchemy-модели
├── timer.py         # Логика помодоро
└── main.py          # Точка входа

tests/
└── test_timer.py    # Unit-тесты для таймера

Dockerfile
docker-compose.yml
requirements.txt
README.md


## Запуск unit-тестов

```bash
docker compose exec web pytest -v
```


## 👤 Автор

Irlandxc727
📫 Telegram: @Cednalri
